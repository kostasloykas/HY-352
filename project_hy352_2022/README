Μέλη της ομάδας (Ονοματεπώνυμο και ΑΜ):
Κωνσταντινος Λουκας ΑΜ:3980


Όνομα IDE και version της C++ που χρησιμοποιήσατε:
IDE: Visual Studio Code version 1.73.1
version C++ standard
g++ version στο υπολογιστη μου (Ubuntu 9.4.0-1ubuntu1~20.04.1) 9.4.0



Ποια διαδικασία ακολουθείτε για να τρέξετε το project σας (καλό θα ήταν να έχετε και Makefile/CMake):
Εχω γραψει ενα Makefile το οποιο εχει τις εξης ενεργειες:
    1.make run το οποιο κανει clean τα αρχεια , compile και μετα εκτελει το εκτελεσιμο αυτοματα.
    2.make compile κανει compile τα αρχεια.
    3.make clean καθαριζει τα αρχεια που εχουν δημιουργηθει κατα το compile.

    
    
Τι υλοποίησατε και τι όχι από το ζητούμενα της εκφώνησης
    Τα υλοποιησα ολα εκτος αν μου ξεφυγε κατι.
    Το μονο διαφορετικο που εκανα ειναι στο συντακτικο οτι ΜΟΝΟ στον operator+ μεταξυ arrays 
    και objects αντιστοιχα πρεπει τα arrays και τα objects να ειναι μεσα σε παρενθεσεις για να λειτουργησει.
    Για παραδειγμα:
    JSON(hy352_ang) = (ARRAY[ NUMBER(1), NUMBER(2)]) + (ARRAY[ NUMBER(3),NUMBER(4)])
    JSON(hy352_ang) = (OBJECT{KEY(3980) : STRING("loukas")}) + (OBJECT{KEY(kostas) : STRING("loukas")})
    
    Επισης κατι που πρατηρησα τωρα αφου τελειωσα το project την υλοποιηση του object δεν την εκανα 
    με hashmap οποτε μην προσπαθησετε να βαλετε δυο φορες το ιδιο key name στο ιδιο object.

    
    
Το μηχάνημα της σχολής που το τρέξατε και το ελέγξατε:
Στον υπολογιστη frapa ετρεξα το project
g++ version στο υπολογιστη της σχολης frapa (Debian 10.2.1-6) 10.2.1
